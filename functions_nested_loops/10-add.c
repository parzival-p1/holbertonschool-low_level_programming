#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @n: int to be added to m
 * @m: int to be added to n
 * Return: sum of n + m
 */

int add(int n, int m)
{
	int n;

	o = n + m;

	return (o);
}
